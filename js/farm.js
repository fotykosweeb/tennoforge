const $=s=>document.querySelector(s);
// esc() comes from app.js, which every page loads before this script.
async function run(){const q=$('#farmSearch').value.trim();if(!q)return;$('#farmResult').innerHTML='<div class="notice">Querying live drop tables…</div>';try{const r=await fetch('/api?route=drops&q='+encodeURIComponent(q));if(!r.ok)throw Error();const j=await r.json(),rows=j.data||[];$('#farmResult').innerHTML=`<div class="panel"><div class="eyebrow">LIVE DROP DATA</div><h2>${esc(q)}</h2><p class="sub">Drop chances are taken from the public WFCD dataset, which is parsed from Digital Extremes' official drop data.</p>${rows.length?`<table class="table"><thead><tr><th>Item</th><th>Source / location</th><th>Chance</th><th>Rarity</th></tr></thead><tbody>${rows.slice(0,120).map(x=>`<tr><td>${esc(x.item||'—')}</td><td>${esc(x.place||'—')}</td><td>${x.chance==null?'—':esc(x.chance)+'%'}</td><td>${esc(x.rarity||'—')}</td></tr>`).join('')}</tbody></table>`:`<div class="notice">${j.note?esc(j.note):'No matching drop source was found in the live dataset.'}</div>`}</div>`}catch(e){$('#farmResult').innerHTML='<div class="notice">Drop data request failed. No demo result was substituted.</div>'}}$('#farmGo').onclick=run;$('#farmSearch').addEventListener('keydown',e=>{if(e.key==='Enter')run()});const q=new URLSearchParams(location.search).get('q');if(q){$('#farmSearch').value=q;run()}